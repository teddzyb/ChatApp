<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ChatApp.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:AuthView="clr-namespace:ChatApp.Views.Auth"
    xmlns:TabbedView="clr-namespace:ChatApp.Views.Tabbed">

    <ContentPage.Content>
        <StackLayout>

            <AuthView:Login x:Name="Login" IsVisible="False" />

            <AuthView:Signup x:Name="Signup" IsVisible="False" />

            <AuthView:ResetPass x:Name="ResetPass" IsVisible="True" />

            <TabbedView:Chat x:Name="Chat" IsVisible="False" />

            <TabbedView:Profile x:Name="Profile" IsVisible="False" />

        </StackLayout>

        <!--<StackLayout VerticalOptions="End" IsVisible="True">
                <Grid BackgroundColor="#e91e63">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackLayout Grid.Row="0" Grid.Column="0" HorizontalOptions="CenterAndExpand" Margin="0, 5, 0, 5">
                        <Image Source="chat.png" HeightRequest="32" />
                        <Label TextColor="#f8f9fa">Chat</Label>
                        <StackLayout.GestureRecognizers>
                            <TapGestureRecognizer NumberOfTapsRequired="1" />
                        </StackLayout.GestureRecognizers>
                    </StackLayout>

    <NavigationPage.TitleView>
        <StackLayout Orientation="Horizontal" Margin="0, 0, 20, 0" IsVisible="True">

            <Button Text="Chat" />
            <Button Text="Profile" />

        </StackLayout>-->
    </ContentPage.Content>

</ContentPage>
